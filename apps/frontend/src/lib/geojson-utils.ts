/** Compatibility re-export; shared route geometry helpers live in map-runtime. */
export {
  extractLineCoordinates,
  unwrapGeometry,
  type AnyGeoJSON,
  type BareGeometry,
  type FeatureGeometry,
  type LatLng,
} from '@nearventure/map-runtime/geojson-utils';
