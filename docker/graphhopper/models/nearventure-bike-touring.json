{
  "distance_influence": 45,
  "priority": [
    { "if": "road_class == CYCLEWAY", "multiply_by": "1.5" },
    { "if": "road_class == RESIDENTIAL || road_class == LIVING_STREET", "multiply_by": "1.2" },
    { "if": "road_class == TERTIARY", "multiply_by": "1.1" },
    { "if": "road_class == SECONDARY", "multiply_by": "0.65" },
    { "if": "road_class == PRIMARY", "multiply_by": "0.3" },
    { "if": "road_class == TRUNK", "multiply_by": "0.1" },
    { "if": "surface == ASPHALT || surface == PAVED || surface == CONCRETE", "multiply_by": "1.15" },
    { "if": "surface == GRAVEL || surface == FINE_GRAVEL || surface == COMPACTED", "multiply_by": "0.85" },
    { "if": "surface == DIRT || surface == GROUND", "multiply_by": "0.55" },
    { "if": "surface == SAND", "multiply_by": "0.1" },
    { "if": "track_type == GRADE4 || track_type == GRADE5", "multiply_by": "0.55" },
    { "if": "average_slope > 10", "multiply_by": "0.65" }
  ]
}
