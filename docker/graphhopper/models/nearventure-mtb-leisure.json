{
  "distance_influence": 45,
  "priority": [
    { "if": "road_class == PATH", "multiply_by": "1.2" },
    { "if": "road_class == TRACK", "multiply_by": "1.25" },
    { "if": "road_class == FOOTWAY", "multiply_by": "1.1" },
    { "if": "road_class == PRIMARY || road_class == TRUNK", "multiply_by": "0.2" },
    { "if": "surface == GRAVEL || surface == FINE_GRAVEL || surface == DIRT || surface == GROUND", "multiply_by": "1.2" },
    { "if": "surface == ASPHALT || surface == CONCRETE || surface == PAVED", "multiply_by": "0.7" },
    { "if": "mtb_rating > 3", "multiply_by": "0.2" },
    { "if": "mtb_rating > 2", "multiply_by": "0.65" },
    { "if": "average_slope > 12", "multiply_by": "0.55" }
  ]
}
